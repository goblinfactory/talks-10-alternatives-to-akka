<Query Kind="Expression" />

// console app to consume an IAsynEnumerable feed from a webapi
// don't know if we need this now, as that might mean we have console app -> webapi (quote-panel) -> external API's.

// should start with the acceptance test? define that ... and then get two different solutions ...



